version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "window.env = { VITE_API_ENDPOINT: '${VITE_API_ENDPOINT}' };" > config.js
    build:
      commands:
        - echo "No build step needed for static HTML"
  artifacts:
    baseDirectory: ./
    files:
      - '**/*'
  cache:
    paths: []