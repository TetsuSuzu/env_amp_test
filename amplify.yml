version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "window.env = {" > config.js
        - echo "  VITE_API_ENDPOINT: "${VITE_API_ENDPOINT}"" >> config.js
        - echo "};" >> config.js
    build:
      commands:
        - echo "No build step needed for static HTML"
  artifacts:
    baseDirectory: ./
    files:
      - '**/*'
  cache:
    paths: []
